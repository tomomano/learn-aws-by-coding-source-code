```
$ export ENDPOINT_URL="https://api.neuralartcanvas.org"

# Post a single job
$ python client.py -s picasso.png -c dancing.png single

# Post many jobs
$ python client.py -s picasso.png -c dancing.png many 10
```
